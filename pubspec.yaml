name: live_streaming
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_webrtc: ^0.9.46
  google_fonts: ^6.1.0
  google_nav_bar: ^5.0.6
  image_picker: ^1.0.7
  path_provider: ^2.1.2
  permission_handler: ^11.0.1
  socket_io_client: ^2.0.3+1
  web3dart: ^2.7.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/stream_sdk.json
    - assets/logo.png
